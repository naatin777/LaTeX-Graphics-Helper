name: Check

on:
    pull_request:
    push:
        branches:
            - main

concurrency:
    group: check-${{ github.ref }}
    cancel-in-progress: true

jobs:
    check:
        runs-on: ubuntu-latest
        env:
            LGH_TASK_BASE: ${{ github.event.pull_request.base.sha || github.event.before }}
        steps:
            - uses: actions/checkout@v7

            - run: corepack enable
              shell: bash

            - run: corepack prepare pnpm@11.8.0 --activate
              shell: bash

            - uses: actions/setup-node@v6
              with:
                  node-version: '22'
                  cache: pnpm

            - run: pnpm install --frozen-lockfile
              shell: bash

            - run: pnpm run check:all
              shell: bash
