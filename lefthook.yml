pre-commit:
  commands:
    lint:
      glob: "**/*.{ts,tsx}"
      run: pnpm exec oxlint {staged_files}
    format:
      glob: "**/*.{ts,tsx}"
      run: pnpm exec oxfmt --check {staged_files}
    typecheck:
      run: pnpm run typecheck
