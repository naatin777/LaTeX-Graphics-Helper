pre-commit:
  commands:
    lint:
      glob: "**/*.{ts,tsx}"
      run: npx oxlint {staged_files}
    format:
      glob: "**/*.{ts,tsx}"
      run: npx oxfmt --check {staged_files}
    typecheck:
      run: npx tsc --noEmit
