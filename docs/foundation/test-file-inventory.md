# v1 test evidence file inventory

- 対象: `origin/next/v1` at `75ca52a`, local worktree `audit/v1-test-evidence-inventory`
- 状態: 監査用baseline。仕様の正本、runner採用、test移行を決定する文書ではない
- 監査日: 2026-07-16

## Inventory method and scope

このinventoryはGitHub検索や既存foundation文書のfile名一覧から作ったものではない。次のlocal repository treeと実行定義を照合した。

```text
git ls-files
git ls-files 'test/**'
git ls-files '*test.ts' '*test.tsx' '*spec.ts' '*spec.tsx' '*test.js' '*spec.js' '*test.mjs' '*spec.mjs'
git ls-files 'test/fixtures/**' 'test/helpers/**' 'test/playwright/**'
git ls-files 'scripts/**' '.github/scripts/**' '.github/workflows/**'
```

Current runnerは`.vscode-test.mjs`、`playwright.config.mjs`、`package.json`、workflowの実行内容から判定した。Required runtimeは、testのassertion、fixture、helper、source dependency graph、process / native dependency / platform前提を確認した上での仮説である。source importだけでは決めていない。

### Counts

| Item                                    |        Count | Evidence                                                                  |
| --------------------------------------- | -----------: | ------------------------------------------------------------------------- |
| Mocha test files by domain              |           61 | `git ls-files 'test/**/*.test.ts'`                                        |
| Browser Playwright spec files           |            0 | Browser runnerは廃止。過去specは履歴として残す                            |
| Electron Playwright spec files          |            1 | `test/playwright/electron/crop_pdf_configure.spec.ts`                     |
| Webview component test files            |            3 | crop、merge、splitのJSDOM test                                            |
| Test implementation files               |       **49** | 45 + 1 Electron + 3 Webview。fixture、helper、snapshotは除外              |
| Statically declared test cases          |      **212** | 207 Host + 1 Electron + 4 Webview。`suite` declarationsは46               |
| Configured VS Code Extension Host scope | 45 / **207** | `.vscode-test.mjs` の `out/test/**/*.test.js`                             |
| Configured Browser Playwright scope     |        0 / 0 | Browser projectは廃止                                                     |
| Configured Electron Playwright scope    |    1 / **1** | `vscode-electron` project                                                 |
| Configured Vitest scope                 |    3 / **4** | crop、merge、splitのapp configとJSDOM component cases                     |
| Tracked test helper files               |            8 | root 4、Electron 4。inline helper functionは含めない                      |
| Tracked fixtures                        |           20 | `test/fixtures/**`                                                        |
| Electron snapshot files                 |            2 | Linux dark/light PNG                                                      |
| Validation scripts                      |            1 | `scripts/check-nls.mjs`                                                   |
| Build/package scripts                   |            1 | `scripts/package-vsix.mjs`                                                |
| Test runner/config files                |            6 | `.vscode-test.mjs`, `playwright.config.mjs`, shared + 3 app Vitest config |
| CI environment/support scripts          |            5 | tool install/verify                                                       |

`test`の対象はExtension Host、`test:webview`の対象はJSDOM component、`test:playwright:vsix`の対象はinstalled VSIX Electron E2Eである。単一のaggregate scriptを「all」と呼ばず、runtimeごとのEvidenceを分離する。実際の3 OS結果はGitHub Actionsで確認する。

## Runtime definitions

| ID  | Current runtime / runner                                                | Contractually required when                                                                                                      | Not sufficient as a classification rule                                                                       |
| --- | ----------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| N   | Node process (Mocha/Vitest candidate)                                   | pure logic/protocol/path、Node filesystem、child process、PDF/image library、failure injection                                   | testが`test/`にあること、`vscode` importがないことだけ                                                        |
| H   | VS Code Extension Host, VS Code `1.128.0`, Mocha via `@vscode/test-cli` | activation、`vscode.commands`、workspace/configuration、provider、notification/progress、`globalState`、VS Code URI/Document API | source moduleのtop-level `vscode` importだけ。contractがpureでもimplementation boundaryで必要になる場合がある |
| B   | Historical Chromium via Browser Playwright                              | PDF.js worker、canvas pixels、DPI、layout、IntersectionObserver、scroll/zoom、browser API                                        | current required runtimeではない                                                                              |
| E   | Playwright Electron experimental API + real VS Code window/Webview      | actual command journey、Host↔Webview bridge、real theme CSS、installed extension journey                                         | pure operation、package内部module import                                                                      |
| P   | Installed VSIX on VS Code Electron + native OS                          | VSIX contents、production dependency、native Sharp、controlled external-fetch/network policy、package discovery                  | development extension pathの成功                                                                              |
| M   | Native OS/platform matrix                                               | Windows path/symlink/process/native ABI、macOS/Linux external tools、packaging target                                            | 1 OSでのplatform simulationだけ                                                                               |

## File-level inventory

略号: `H`/`B`/`E`/`P`はcurrent runner、`N`/`M`はrequired runtime hypothesis。`confirmed`はコードと実行定義の両方を直接確認、`likely`はcontractと依存から高い確度で推定、`uncertain`はimplementation boundaryまたはplatform条件の切り分けが残る状態である。

| Path                                                         | Suite / case groups                                                                                                                        | Current runner / runtime                            | Contract and oracle                                                                                             | Real dependencies / mocked dependencies / platform sensitivity                                                                                                                                                                                    | Required runtime hypothesis / confidence                                                    | Recommendation / evidence gap / related spec                                                                                                                                              |
| ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `test/integration/check_nls.test.ts`                         | `NLS consistency check` / extensionPath取得、`scripts/check-nls.mjs`実行、stdout/stderr                                                    | H / VS Code Host                                    | extension内scriptが英日NLS key、placeholder、manifest参照を検査し成功文言を返す                                 | `vscode.extensions`、Node child process、repository/package files。mockなし。extension path/cwdに依存                                                                                                                                             | N + package-root path。`likely`                                                             | P2 boundary prerequisite: extension path discovery must be separated from the Node validation script. NLS静的整合、packaging spec                                                         |
| `test/operations/cleanup_conversion_artifacts.test.ts`       | `変換artifactのライフサイクル` / preserve、外部symlink、失敗、session ownership                                                            | H / VS Code Host                                    | preserve backup、staging/input copy cleanup、外部symlink/別session/unknown/harness log保護、cleanup失敗の非伝播 | real temp filesystem、symlink、failure injection。Windows symlink権限差                                                                                                                                                                           | N + M。`confirmed`                                                                          | **P0 candidate**。安全性critical。`file-operation-security.md`, `safe-mode.md`                                                                                                            |
| `test/operations/commit_conversion_outputs.test.ts`          | `変換結果の反映処理` / keep-both、batch conflict、cancel、overwrite、rollback、race                                                        | H / VS Code Host                                    | 競合判断1回、backup、atomic-ish commit/rollback、外部変更検出、rollback error保持                               | real filesystem、copy failure/conflict decision/output channel injection。path/locking差                                                                                                                                                          | N + M。`confirmed`                                                                          | **P0 candidate**。dialog UIはmockedで未証明。`safe-mode.md`, `file-operation-security.md`                                                                                                 |
| `test/operations/convert_pdftocairo_ascii_scratch.test.ts`   | `Windows pdftocairo ASCII scratch` / 5 output routes、wrong/empty output                                                                   | H / VS Code Host                                    | Unicode logical pathをASCII scratchへ隔離し、期待形式・logical output・success cleanupを証明                    | real fs、Sharp decode、pdftocairo/outputsはfake、Windows platform/scratch base注入。実tool互換は未証明                                                                                                                                            | N + M。`confirmed`                                                                          | P1 candidate experiment。実pdftocairoはOS CI/verify script側。`external-tool-ascii-scratch.md`                                                                                            |
| `test/operations/convert_to_pdf_operation.test.ts`           | `PDF変換operation（PNG入力）` / real PNG→PDF                                                                                               | H / VS Code Host                                    | fixture PNGをPDFへ変換し、PDF outputが読めること                                                                | real fixture、filesystem、Sharpをsource graphで使用。UIなし                                                                                                                                                                                       | N + native Sharp。`likely`                                                                  | P1 candidate。寸法/content oracleを追加確認する価値。`output-format-conversion.md`                                                                                                        |
| `test/operations/convert_rsvg_ascii_scratch.test.ts`         | `Windows rsvg-convert ASCII scratch` / success、wrong/empty output                                                                         | H / VS Code Host                                    | Unicode input/outputをASCII scratchで処理し、logical outputと失敗時保護                                         | real fs/pdf-lib、rsvg process/output fake、Windows simulation                                                                                                                                                                                     | N + M。`confirmed`                                                                          | P1 candidate。実rsvg互換はtool smoke/OS CIに分離。`external-tool-ascii-scratch.md`                                                                                                        |
| `test/commands/convert_to_avif_command.test.ts`              | `AVIFに変換コマンド` / registration、raster/PDF/SVG/Mermaid input、same-format拒否                                                         | H / VS Code Host                                    | public command、複数入力、形式読み取り、output dimensions、same-format拒否                                      | actual VS Code command/workspace config/URI; real Sharp/pdf-lib/Chrome/tool settings; notifications mocked                                                                                                                                        | H for command contract、N for operation groups。`confirmed`                                 | split responsibility。Draw.io、progress/cancel/Safe Mode UI、pixel oracleはgap。`output-format-conversion.md`                                                                             |
| `test/operations/convert_to_avif_operation.test.ts`          | `AVIFに変換する処理` / editable Draw.io→PDF→PNG→AVIF                                                                                       | H / VS Code Host                                    | injected Draw.io/PDF/PNG routeがAVIFを作り、読めること                                                          | real fs/Sharp、runner injection、PDF/PNG intermediate。native codec/platform差                                                                                                                                                                    | N + native + M。`likely`                                                                    | P1 candidate。実Draw.io CLIと全入力matrixは未証明。`output-format-conversion.md`                                                                                                          |
| `test/commands/convert_to_jpeg_command.test.ts`              | `JPEGに変換コマンド` / registration、raster/PDF/SVG/Mermaid                                                                                | H / VS Code Host                                    | command wiring、読み取り可能JPEG、batchとdimensions                                                             | VS Code command/config/URI、real Sharp/pdftocairo/Chrome、notifications mocked                                                                                                                                                                    | H for command、N for operation。`confirmed`                                                 | keep Host command group / split output group。Draw.io command route、progress/cancel、pixel oracle gap。`output-format-conversion.md`                                                     |
| `test/commands/convert_to_pdf_command.test.ts`               | `PDFに変換コマンド` / registration、raster/SVG/Mermaid、config precedence、batch、unsupported/same format                                  | H / VS Code Host                                    | public command、PDF page count/size、input validation、output template precedence                               | VS Code command/config/URI、real Sharp/pdf-lib/Chrome、notifications mocked。Draw.io詳細は未対象                                                                                                                                                  | H for command, N for pure operation groups。`confirmed`                                     | mixed: command integration keep; format/backend groups candidate experiment。Draw.io actual command, cancellation/UI gap。`output-format-conversion.md`                                   |
| `test/operations/convert_to_pdf_drawio_path.test.ts`         | `Draw.ioの複雑なpath変換` / 3-page result, spaces/Unicode                                                                                  | H / VS Code Host                                    | fake runnerへlogical pathを渡し、fixture page count/sizeを保持                                                  | real fixture/fs/pdf-lib、Draw.io runner fake                                                                                                                                                                                                      | N + external runner boundary。`confirmed`                                                   | P1 candidate。実Draw.io CLIは未証明。`output-format-conversion.md`                                                                                                                        |
| `test/commands/convert_to_png_command.test.ts`               | `PNGに変換コマンド` / registration、raster/PDF/SVG/Mermaid、output path、same-format拒否                                                   | H / VS Code Host                                    | command wiring、readable PNG、dimensions、page output                                                           | VS Code command/config/URI、real Sharp/pdftocairo/Chrome、notifications mocked                                                                                                                                                                    | H for command, N for operation groups。`confirmed`                                          | mixed. Draw.io actual command, cancellation/progress/Safe Mode UI, pixel oracle gap。`output-format-conversion.md`                                                                        |
| `test/operations/convert_to_png_drawio_route.test.ts`        | `PNGに変換するDraw.io経路` / source text assertions                                                                                        | H / VS Code Host                                    | implementationにDraw.io→PDF routeがあり、PNG direct flagがないこと                                              | Node fs/source text only。no runtime API; brittle source oracle                                                                                                                                                                                   | N。`likely`                                                                                 | candidate experiment, but architecture/source-text assertion is an evidence quality issue. `output-format-conversion.md`                                                                  |
| `test/operations/convert_to_png_operation.test.ts`           | `PNGに変換する処理` / editable Draw.io→PDF→PNG                                                                                             | H / VS Code Host                                    | injected runner routeがreadable PNGを作ること                                                                   | real fs/pdf-lib/Sharp, runner injection                                                                                                                                                                                                           | N + native + M。`likely`                                                                    | P1 candidate. actual Draw.io tool and route failure not fully covered. `output-format-conversion.md`                                                                                      |
| `test/commands/convert_to_svg_command.test.ts`               | `SVGに変換コマンド` / registration、PDF/Mermaid、output path                                                                               | H / VS Code Host                                    | command wiring、readable SVG, page outputs, template precedence                                                 | VS Code command/config/URI、real pdftocairo/Chrome, notifications mocked                                                                                                                                                                          | H for command, N for operation groups。`confirmed`                                          | split. Draw.io actual route, Mermaid settings/theme, progress/cancel gap. `output-format-conversion.md`                                                                                   |
| `test/operations/convert_to_svg_operation.test.ts`           | `SVGに変換する処理` / Draw.io success, Draw.io/pdftocairo errors                                                                           | H / VS Code Host                                    | staging→output and stderr-preserving error mapping                                                              | real fs, fake Draw.io/pdftocairo runners, no VS Code graph                                                                                                                                                                                        | N + external process boundary。`confirmed`                                                  | P1 candidate. actual tool/platform behavior absent. `output-format-conversion.md`                                                                                                         |
| `test/commands/convert_to_webp_command.test.ts`              | `WebPに変換コマンド` / registration、raster/PDF/SVG/Mermaid、same-format拒否                                                               | H / VS Code Host                                    | command wiring、readable WebP、batch/dimensions                                                                 | VS Code command/config/URI、real Sharp/pdftocairo/Chrome, notifications mocked                                                                                                                                                                    | H for command, N for operation groups。`confirmed`                                          | split. effort/cancellation/Safe Mode UI and pixel quality gap. `output-format-conversion.md`                                                                                              |
| `test/operations/convert_to_webp_operation.test.ts`          | `WebPに変換する処理` / Draw.io→PDF→PNG→WebP                                                                                                | H / VS Code Host                                    | intermediate route and readable output                                                                          | real fs/Sharp, runner injection                                                                                                                                                                                                                   | N + native + M。`likely`                                                                    | P1 candidate. actual Draw.io/tool route absent. `output-format-conversion.md`                                                                                                             |
| `test/operations/crop_pdf_auto.test.ts`                      | `PDF自動crop処理` + `BoundingBoxのparse処理` / PDF operation, workspace preflight, cancel, external errors, parser                         | H / VS Code Host                                    | bbox, pdf-lib crop, p-limit, workspace rejection, cancellation, output/diagnostic errors                        | real fs/pdf-lib, fake Ghostscript bbox/process, AbortSignal/p-limit. UI absent                                                                                                                                                                    | N + injected Ghostscript; H only for command. `confirmed`                                   | split candidate; no `cropPdf.auto` command picker/execute test. `auto-crop.md`, cancellation spec                                                                                         |
| `test/operations/crop_pdf_configure_operation.test.ts`       | `PDF configure crop処理` / all/selected, Unicode/template, visual content                                                                  | H / VS Code Host                                    | crop boxes/page preservation/output path and rendered PDF comparison                                            | real fixture/fs/pdf-lib/Sharp; `vscode.workspace` only for pdftocairo setting; real pdftocairo                                                                                                                                                    | N + configured renderer, current H is harness convenience。`confirmed`                      | P2 boundary prerequisite: renderer setting ownership must be separated before a Node experiment. file comment says no Webview/command bridge. `crop-pdf-configure.md`                     |
| `test/operations/crop_pdf_ghostscript_ascii_scratch.test.ts` | `Windows Ghostscript ASCII scratch` / success, failure, cancel                                                                             | H / VS Code Host                                    | scratch isolation, logical path, diagnostic retention                                                           | real fs/pdf-lib, fake Ghostscript, Windows/scratch base simulation                                                                                                                                                                                | N + M。`confirmed`                                                                          | P1 candidate. fallback/symlink base and real Ghostscript compatibility gaps. `external-tool-ascii-scratch.md`                                                                             |
| `test/commands/crop_pdf_output_path_validation.test.ts`      | `PDF crop outputPath検証` / preflight rejects NUL before progress/staging                                                                  | H / VS Code Host                                    | command boundary prevents progress and file creation                                                            | VS Code command/workspace, Sinon stubs, real temp fs                                                                                                                                                                                              | H. `confirmed`                                                                              | keep Host; narrow command preflight. full invalid-input matrix is in pure path tests, not here. `output-path-validation.md`                                                               |
| `test/application/crop_pdf_protocol.test.ts`                 | `Crop PDF Webview protocol` / valid apply, preview error vs invalid apply                                                                  | H / VS Code Host                                    | pure message shape validation oracle                                                                            | Node assert and protocol module only; no browser/Host API                                                                                                                                                                                         | N. `confirmed`                                                                              | **P0 candidate**. no malformed-message matrix beyond two cases. `crop-pdf-configure.md`                                                                                                   |
| `test/integration/extension.test.ts`                         | `拡張機能の基本動作` / extension discovery/activation, registrations, one PNG→PDF execution                                                | H / VS Code Host                                    | actual activation and command registry; one public command journey                                              | real VS Code extension registry/workspace/URI; notifications mocked in execution case                                                                                                                                                             | H. `confirmed`                                                                              | keep Host but split activation/registry/command execution. only subset of public commands executed. `output-format-conversion.md`                                                         |
| `test/operations/file_content_hash.test.ts`                  | `大きなファイルの内容比較` / same/different streaming hash                                                                                 | H / VS Code Host                                    | hash equality and mismatch on real 1MiB files                                                                   | Node fs/temp files; no mocks; filesystem performance                                                                                                                                                                                              | N + M. `confirmed`                                                                          | **P0 candidate**. no very-large/permission/changed-during-read oracle. `undo-last-conversion.md`                                                                                          |
| `test/edit_provider/latex_drop_edit_provider.test.ts`        | `LaTeXファイルdrag挿入` / single/multi URI, parsing, invalid all-or-nothing, external PDF relative path, cancel/unsaved                    | H / VS Code Host                                    | `DocumentDropEditProvider` response, `SnippetString`, URI/path semantics, cancellation                          | VS Code TextDocument/URI/DocumentDropEdit/Drop DataTransfer/fs; real PDF fixture; no UI mock                                                                                                                                                      | H. `confirmed`                                                                              | keep Host provider group; parser/snippet pure split candidate. UNC/platform gap documented in spec. `latex-insertion.md`                                                                  |
| `test/edit_provider/latex_paste_edit_provider.test.ts`       | `LaTeXクリップボード画像挿入` / image/PDF choice, InputBox/QuickPick, commit/Undo/cancel                                                   | H / VS Code Host                                    | `DocumentPasteEditProvider` lifecycle, `DataTransfer`, `SnippetString`, output/artifact ownership               | VS Code workspace.fs/document/URI/Range/SnippetString/CancellationToken; UI choice and notifications mocked; real fs/pdf/PNG                                                                                                                      | H. `confirmed`                                                                              | keep provider integration; responsibility mixed with conversion/commit/Undo. no physical clipboard or UI rendering. `latex-insertion.md`                                                  |
| `test/edit_provider/latex_snippet.test.ts`                   | `LaTeX snippet option builder` / one alignment choice case                                                                                 | H / VS Code Host                                    | snippet choice text for pure builder behavior                                                                   | source `LatexSnippet` constructs real `vscode.SnippetString`; no direct test API mock                                                                                                                                                             | H currently; contract N but implementation graph H. `uncertain`                             | P2 boundary prerequisite: `SnippetString` ownership must be decided before a Node experiment; import presence alone is not sufficient evidence. `latex-insertion.md`                      |
| `test/commands/merge_pdf_command.test.ts`                    | `PDF結合コマンド` / registry, selection validation, save dialog, output, failure                                                           | H / VS Code Host                                    | public command input validation, selection order, notifications, rendered output                                | VS Code commands/URI/workspace/save dialog/notifications mocked; real PDF fixture/pdftocairo/Sharp visual helper                                                                                                                                  | H for command journey; N for operation/render if tool path injected。`confirmed`            | split command and visual oracle. dedicated merge spec missing. `pdf-operation-command-modes.md`                                                                                           |
| `test/operations/merge_pdf_operation.test.ts`                | `PDF結合operation` / staging, Safe Mode, Undo, cancel                                                                                      | H / VS Code Host                                    | operation output/transaction and Undo behavior                                                                  | real fs/pdf fixture; imports `rememberLastConversion` from command module, creating transitive H; Safe Mode decisions injected                                                                                                                    | N contractually after record owner split; current H. `uncertain`                            | P2 boundary prerequisite: command record ownership must be separated before a Node experiment; hidden dependency is an architecture issue, not a Host contract. merge spec / safety specs |
| `test/commands/output_conversion_messages.test.ts`           | `出力形式変換commandのmessage` / locale message keys/values                                                                                | H / VS Code Host                                    | message key and interpolation mapping                                                                           | source `localeMap` reads `vscode.env.language`; assert only; no UI                                                                                                                                                                                | H currently; N only with locale injection. `confirmed`                                      | P2 boundary prerequisite: locale/environment ownership must be injected or moved before a Node experiment. `output-format-conversion.md`                                                  |
| `test/integration/package_manifest.test.ts`                  | `package.jsonの変換メニュー定義` / public IDs, menus, NLS, activation/config                                                               | H / VS Code Host                                    | package manifest consistency, legacy/manual absence, context clauses                                            | real package/NLS fs; imports `PUBLIC_COMMAND_IDS` from `extension.ts`, transitive H                                                                                                                                                               | N contract, H current; `confirmed`                                                          | P2 boundary prerequisite: manifest constant ownership and packaging boundary must be separated before a Node experiment. packaging/migration specs                                        |
| `test/operations/png_safe_mode.test.ts`                      | `PNG変換のSafe Mode` / staging batch, all conflict choices, failure/cancel, Undo, Draw.io route                                            | H / VS Code Host                                    | batch atomicity, conflict decision, backup/restore, cancellation                                                | real fs/pdf/Sharp; Safe Mode and Draw.io runners injected; no VS Code graph; no UI/progress                                                                                                                                                       | N + native + M. `confirmed`                                                                 | P1 candidate. only PNG-specific end-to-end safety Evidence; other output commands rely on shared implementation. `safe-mode.md`, cancellation spec                                        |
| `test/config/resolve_output_path.test.ts`                    | `出力パスのテンプレート解決` / source variables, relative path, recursive substitution, Windows/POSIX rules                                | H / VS Code Host                                    | resolved/ rejected output path under injected platform                                                          | pure path function, platform passed by cast; no fs/VS Code                                                                                                                                                                                        | N + M. `confirmed`                                                                          | **P0 candidate**. implementation test calls an asserted cast for platform option; API contract should be checked in experiment. `output-path-validation.md`                               |
| `test/operations/run_external_tool.test.ts`                  | `外部tool runner` / args, redaction, stdout                                                                                                | H / VS Code Host                                    | child process output/log redaction                                                                              | real Node child process (`process.execPath`); output channel fake; **test title says signal but no AbortSignal is passed**                                                                                                                        | N + M. `confirmed`                                                                          | **P1 experiment candidate**: real child process, platform behavior, and missing cancellation oracle require comparison. `external-tool-ascii-scratch.md`                                  |
| `test/operations/run_staged_conversion_batch.test.ts`        | `staged conversion batch` / success lifecycle, stage failure cleanup/no final output                                                       | H / VS Code Host                                    | stage-all-before-commit and operation-root cleanup                                                              | real fs/temp; stage/commit/undo callbacks injected; no VS Code graph                                                                                                                                                                              | N + M. `confirmed`                                                                          | **P0 candidate**. rollback/commit failure phases are covered elsewhere, not all here. safety specs                                                                                        |
| `test/application/safe_mode.test.ts`                         | `Safe Mode状態` / initial ON, persisted toggle                                                                                             | H / VS Code Host                                    | pure state and key-value persistence                                                                            | in-memory globalState fake; no VS Code graph                                                                                                                                                                                                      | N. `confirmed`                                                                              | **P0 candidate**. actual globalState/profile persistence and command are separate Host tests. `safe-mode.md`                                                                              |
| `test/commands/safe_mode_dialog.test.ts`                     | `Safe Modeダイアログの判断` / keep/cancel/overwrite/close/OFF/batch count                                                                  | H / VS Code Host                                    | decision mapping from `showWarningMessage` result                                                               | VS Code window/commands/status bar/globalState mocked with Sinon; no actual dialog UI                                                                                                                                                             | H API surface, but oracle is harness-only. `confirmed`                                      | keep as Host API adapter test; do not call it visual evidence. `safe-mode.md`                                                                                                             |
| `test/commands/safe_mode_status_bar.test.ts`                 | `Safe Modeステータスバー` / item creation/text/toggle/restore/disposables                                                                  | H / VS Code Host                                    | mocked StatusBarItem/command registration lifecycle                                                             | VS Code window/commands/context mocked with Sinon; no actual workbench rendering or restart                                                                                                                                                       | H API surface; actual UI evidence absent. `confirmed`                                       | keep adapter test; actual status bar visual/real workbench not tested. `safe-mode.md`                                                                                                     |
| `test/operations/save_clipboard_image.test.ts`               | `Clipboard画像保存のartifact ownership` / rollback failure preserves recovery backup and scopes cleanup                                    | H / VS Code Host                                    | operation artifact ownership after failed rollback                                                              | real fs/temp; conversion/commit failure injection; no clipboard API                                                                                                                                                                               | N + M. `confirmed`                                                                          | P1 candidate. provider choice and `DataTransfer` are elsewhere; one failure case only. `latex-insertion.md`, safety specs                                                                 |
| `test/application/source_format.test.ts`                     | `source format判定` / extension and editable Draw.io compound extension                                                                    | H / VS Code Host                                    | pure format classification and logical source path                                                              | Node assert only                                                                                                                                                                                                                                  | N. `confirmed`                                                                              | **P0 candidate**. one case covers multiple values; failure diagnosis is good. `output-format-conversion.md`                                                                               |
| `test/operations/split_pdf_all_pages.test.ts`                | `PDF全ページ分割` / per-page output, atomic batch, existing/duplicate/cancel, rendered comparison                                          | H / VS Code Host                                    | split output and PDF visual similarity                                                                          | real fs/pdf fixture/pdf-lib; visual helper invokes configured `vscode.workspace` pdftocairo and Sharp; no command URI selection                                                                                                                   | N + configured renderer; current H due helper. `confirmed`                                  | P2 boundary prerequisite: renderer setting ownership must be separated before a Node experiment. no `splitPdf.allPages` command journey. `split-pdf-all-pages.md`                         |
| `test/operations/undo_last_conversion.test.ts`               | `直前変換の取り消し処理` / record lifecycle, hash/absence/symlink/change/restore                                                           | H / VS Code Host                                    | all-or-nothing safe Undo and backup restore                                                                     | real fs/symlink/streaming hash; imports command `rememberLastConversion`, so transitive H; no notification UI                                                                                                                                     | N operation contract after record owner split; current H. `uncertain`                       | P2 boundary prerequisite: command record ownership must be separated before a Node experiment; command notification/newer-id contract is not tested here. `undo-last-conversion.md`       |
| `test/presentation/webview_html.test.ts`                     | `Webview HTML生成` / CSP source, PDF/worker URLs, nonce/style/worker directives                                                            | H / VS Code Host                                    | generated HTML/CSP string                                                                                       | fake `vscode.Webview`, real `vscode.Uri.file`, source HTML builder                                                                                                                                                                                | N possible for string contract; H if VS Code URI semantics is part of contract. `uncertain` | P2 boundary prerequisite: URI/CSP ownership must be decided before a Node experiment; no actual Webview load/CSP enforcement. `crop-pdf-configure.md`, security spec                      |
| `test/security/workspace_path.test.ts`                       | `workspaceパスの安全性` / existing/new path, prefix sibling, symlink, symlink workspace                                                    | H / VS Code Host                                    | logical + realpath workspace boundary                                                                           | real temp dirs, symlink/junction depending on OS; no mocks                                                                                                                                                                                        | N + M. `confirmed`                                                                          | **P0 candidate**. race/ACL behavior explicitly not tested by file comment. `file-operation-security.md`                                                                                   |
| `test/playwright/webview-pdf-rendering.spec.ts`              | Playwright root / 18 statically declared Browser cases: canvas, PDF.js, lazy render, DPI, layout, zoom, scroll, assets, simulated messages | B / Chromium                                        | DOM/canvas/browser rendering and message recorder oracles; local HTTP fixture server                            | real Chromium, PDF.js worker/assets, canvas pixels, in-process HTTP server, generated PDFs, fixed crop fixture; Host API simulated by `acquireVsCodeApi` fake; no screenshot                                                                      | B for renderer, N for asset existence case. `confirmed`                                     | keep Browser oracle case-by-case; host-simulation cases overlap E. Full assertion matrix in `browser-electron-overlap.md`. `crop-pdf-configure.md`                                        |
| `test/playwright/electron/crop_pdf_configure.spec.ts`        | Playwright root / 1 statically declared case with journey, theme, screenshot, output, packaging, native/internal smoke                     | E / VS Code Electron; P when `LGH_VSIX_PATH` is set | actual Explorer→command→Webview→Apply→PDF plus theme/screenshot and packaged smoke sub-oracles                  | VS Code download 1.128.0, Electron, temp workspace/user data/extensions, PDF.js canvas, Sharp screenshot helper, VSIX install, direct packaged module imports, controlled external fetch failure, PDF output; no simulated Host bridge in journey | E for journey/theme; P+M for packaged/native groups. `confirmed`                            | split selection required before any removal/required-scope decision. Packaging evidence is real but mixed with user journey. `crop-pdf-configure.md`, `packaging.md`                      |

## Case extraction index

The following is the direct `suite(...)` / `test(...)` extraction from the current files. It is included to check filename-to-content alignment and overly broad test names; it is not a new test taxonomy.

### Root Mocha files

- `check_nls.test.ts` — `NLS consistency check`: 英日NLSのkeyとplaceholderを検証できる。
- `cleanup_conversion_artifacts.test.ts` — `変換artifactのライフサイクル`: Undo用backupを残してstaging結果と入力コピーを削除する; workspace外へ解決するsymlinkをcleanupしない; cleanup失敗を成功結果へ伝播させずworkspace内の出力を維持する; operation cleanupは別session・未知directory・harness log・symlinkを削除しない。
- `commit_conversion_outputs.test.ts` — `変換結果の反映処理`: 両方残す場合は最小の連番suffixで保存する; 複数の競合があっても判断は1回だけ求める; 上書きしない判断の場合はどの出力も反映しない; 上書き前に既存ファイルをバックアップする; 1件目の成功後に2件目のcopyが失敗すると両方を元へ戻す; overwrite中の現在出力のcopy失敗でもbackupから復元する; 新規出力のcopy失敗では不完全ファイルを残さない; rollback失敗は元エラーと対象pathを保持しOutput Channelへ記録する; commit済み出力へのcancelでもrollbackする; conflict判断後に変更された出力は上書きしない; rollbackは対象外のファイルを削除しない; Safe Modeの取消はAbortErrorとして確認できる。
- `convert_pdftocairo_ascii_scratch.test.ts` — `Windows pdftocairo ASCII scratch`: Unicode論理pathを維持して各routeへ変換し、成功後にscratchを削除する; 期待pathと異なる別名PNGを成功扱いせず、論理出力を作らない; 期待pathの0 byte PNGを成功扱いせず、論理出力を作らない。
- `convert_to_pdf_operation.test.ts` — `PDF変換operation（PNG入力）`: PNGをPDFへ変換する。
- `convert_rsvg_ascii_scratch.test.ts` — `Windows rsvg-convert ASCII scratch`: Unicode論理pathを維持してPDFへ変換し、成功後にscratchを削除する; 期待pathと異なる別名PDFを成功扱いせず、論理出力を作らない; 期待pathの0 byte PDFを成功扱いせず、論理出力を作らない。
- `convert_to_avif_command.test.ts` — `AVIFに変換コマンド`: コマンドが登録されている; PNG、JPEG、WebP、PDFを1つのbatchでAVIFへ変換する; SVGを読み取り可能なAVIFへ変換する; `.mmd`ファイルを読み取り可能なAVIFへ変換する; `.mermaid`ファイルを読み取り可能なAVIFへ変換する; AVIFからAVIFへは変換しない。
- `convert_to_avif_operation.test.ts` — `AVIFに変換する処理`: 編集可能なDraw.io画像はPDFとPNGを経由してAVIFへ変換する。
- `convert_to_jpeg_command.test.ts` — `JPEGに変換コマンド`: コマンドが登録されている; PNG、WebP、AVIF、PDFを1つのbatchでJPEGへ変換する; SVGを読み取り可能なJPEGへ変換する; `.mmd`ファイルを読み取り可能なJPEGへ変換する; `.mermaid`ファイルを読み取り可能なJPEGへ変換する。
- `convert_to_pdf_command.test.ts` — `PDFに変換コマンド`: コマンドが登録されている; PNG、JPEG、WebP、AVIFを1つのbatchでPDFへ変換する; SVGをSVGの幅と高さと同じpointサイズの1ページPDFへ変換する; `.mmd`ファイルを読み取り可能なPDFへ変換する; `.mermaid`ファイルを読み取り可能なPDFへ変換する; `outputPath.convertToPdf`が設定されている場合はペア別設定より優先してpageを展開する; 空文字/空白/未設定時はペア別設定へfallbackする; 大文字拡張子のファイルを変換する; editable Draw.io画像では出力テンプレートに論理入力パスを使う; 複数PNGを1つのバッチとして変換する; 非対応入力が含まれる場合はどのファイルも変換しない; PDFからPDFへは変換しない。
- `convert_to_pdf_drawio_path.test.ts` — `Draw.ioの複雑なpath変換`: ページ名・フォルダ名・ファイル名に空白とUnicodeがあっても3ページPDFへ変換する。
- `convert_to_png_command.test.ts` — `PNGに変換コマンド`: コマンドが登録されている; JPEG、WebP、AVIF、PDFを1つのbatchでPNGへ変換する; SVGを読み取り可能なPNGへ変換する; `.mmd`/`.mermaid`を読み取り可能なPNGへ変換する; `outputPath.convertToPng`の優先とpage展開; PNGからPNGへは変換しない。
- `convert_to_png_drawio_route.test.ts` — `PNGに変換するDraw.io経路`: Draw.io CLIへPNG直接出力を要求せずPDFを経由する。
- `convert_to_png_operation.test.ts` — `PNGに変換する処理`: 編集可能なDraw.io画像はPDFを経由してPNGへ変換する。
- `convert_to_svg_command.test.ts` — `SVGに変換コマンド`: コマンドが登録されている; PDFをページごとのSVGへ変換する; `.mmd`/`.mermaid`をSVGへ変換する; `outputPath.convertToSvg`優先とpage展開。
- `convert_to_svg_operation.test.ts` — `SVGに変換する処理`: editable Draw.ioをDraw.io CLIでSVGへ変換する; Draw.io CLI/pdftocairoの失敗をstderrつきで包む。
- `convert_to_webp_command.test.ts` — `WebPに変換コマンド`: コマンドが登録されている; PNG、JPEG、AVIF、PDFを1つのbatchでWebPへ変換する; SVG/`.mmd`/`.mermaid`を読み取り可能なWebPへ変換する; WebPからWebPへは変換しない。
- `convert_to_webp_operation.test.ts` — `WebPに変換する処理`: editable Draw.ioをPDFとPNGを経由してWebPへ変換する。
- `crop_pdf_auto.test.ts` — `PDF自動crop処理`: bbox取得と全page crop; 空白page; p-limit; existing output; workspace preflight; cancel開始/実行中/queue; Ghostscript missing/failure/log。`BoundingBoxのparse処理`: HiResBoundingBoxをparseする。
- `crop_pdf_configure_operation.test.ts` — `PDF configure crop処理`: 固定fixture全page; selected page; Unicode/complex pathと複数outputPath。
- `crop_pdf_ghostscript_ascii_scratch.test.ts` — `Windows Ghostscript ASCII scratch`: Unicode logical input; failure; cancel。
- `crop_pdf_output_path_validation.test.ts` — `PDF crop outputPath検証`: NUL設定でprogress/stagingを開始しない。
- `crop_pdf_protocol.test.ts` — `Crop PDF Webview protocol`: valid apply payload; preview errorとinvalid applyの区別。
- `extension.test.ts` — `拡張機能の基本動作`: extension registered; activate; auto/configure crop, split, PNG→PDF command registration; manifest/public registration一致; PNG→PDF実行。
- `file_content_hash.test.ts` — `大きなファイルの内容比較`: streaming hashで同一/相違を判定する。
- `latex_drop_edit_provider.test.ts` — `LaTeXファイルdrag挿入`: single/multi PDF; URI list normalization; invalid all-or-nothing; external local PDF relative path; cancel/unsaved document。
- `latex_paste_edit_provider.test.ts` — `LaTeXクリップボード画像挿入`: image/PDF paste; Undo記録失敗; conflict; pre-conflict cancel; conflict-time cancel。
- `latex_snippet.test.ts` — `LaTeX snippet option builder`: figure alignmentのchoice処理。
- `merge_pdf_command.test.ts` — `PDF結合コマンド`: registration; ordered merge; non-PDF/non-file/one-file validation; existing output; mid-merge failure。
- `merge_pdf_operation.test.ts` — `PDF結合operation`: staging/Safe Mode; Undo restore; pre-start cancel; conflict cancel。
- `output_conversion_messages.test.ts` — `出力形式変換commandのmessage`: format/input countからmessage key/valueを組み立てる。
- `package_manifest.test.ts` — `package.jsonの変換メニュー定義`: public/menu/activation IDs; manual/legacy absence; each output submenu; case-insensitive context clauses; NLS; effort; LaTeX activation/settings。
- `png_safe_mode.test.ts` — `PNG変換のSafe Mode`: stage all; batch conflict choices; failure/cancel; overwrite Undo; Draw.io runner.
- `resolve_output_path.test.ts` — `出力パスのテンプレート解決`: source variables; workspace relative path; no recursive expansion; unsupported vars; Windows/POSIX illegal names and Unicode.
- `run_external_tool.test.ts` — `外部tool runner`: args, output, redaction。なおsignalはcase名にあるが実際のcallでは未入力。
- `run_staged_conversion_batch.test.ts` — `staged conversion batch`: success lifecycle; stage failure cleanup/no final output。
- `safe_mode.test.ts` — `Safe Mode状態`: initial ON; persisted toggle。
- `safe_mode_dialog.test.ts` — `Safe Modeダイアログの判断`: Keep Both/Do Not Overwrite/Overwrite/close/OFF/batch count。
- `safe_mode_status_bar.test.ts` — `Safe Modeステータスバー`: item creation/text/toggle/restore/disposables。
- `save_clipboard_image.test.ts` — `Clipboard画像保存のartifact ownership`: rollback failure backup preservation and cleanup scope。
- `source_format.test.ts` — `source format判定`: extension and editable Draw.io compound extension。
- `split_pdf_all_pages.test.ts` — `PDF全ページ分割`: per-page split; batch atomicity; existing/duplicate/cancel。
- `undo_last_conversion.test.ts` — `直前変換の取り消し処理`: record replacement/cleanup; successful delete; changed/missing/symlink protection; backup restore/no restore after change。
- `webview_html.test.ts` — `Webview HTML生成`: PDF.js/worker CSP and source directives。
- `workspace_path.test.ts` — `workspaceパスの安全性`: inside/outside existing/new; prefix sibling; external symlink; symlink workspace。

### Playwright files

- `test/playwright/webview-pdf-rendering.spec.ts` — 18 cases: first-page canvas marker; configure init first page; all pages; lazy distant pages and non-blocking Apply; high-DPI; canvas-only; two-column/zoom controls; zoom size vs PDF points; wheel zoom clamp; pointer-preserving zoom; inner scrolling; auxiliary asset existence; text PDF/auxiliary URLs; selected-page Apply message; Cancel message; invalid inputs without Apply; missing `Map.getOrInsertComputed` fallback; all-page Apply message.
- `test/playwright/electron/crop_pdf_configure.spec.ts` — 1 case: real VS Code Explorer context menu→Crop Configure→Webview all-page Apply→PDF output, dark/light theme/computed styles and snapshots, success notification; packaged mode additionally controlled external-fetch failure, packaged internal operation imports, merge, Sharp PNG→JPEG, missing pdftocairo error.

## Responsibility mixing and stale test-target comments

| Area                                                        | Observation                                                                                                                                                                                                                   | Classification                                                                                              |
| ----------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| Output command files                                        | Each of six command specs combines registration, command input/configuration, conversion output readability, and some path/batch behavior. Draw.io, progress, cancellation, Safe Mode UI are explicitly excluded in comments. | Test architecture issue: split by command adapter vs operation/backend evidence; no migration in this audit |
| `crop_pdf_auto.test.ts`                                     | Operation, parser, workspace preflight, cancellation, external error, and Output Channel are two suites in one file.                                                                                                          | Responsibility mixed but names are mostly truthful                                                          |
| `package_manifest.test.ts`                                  | Manifest/package, migration absence, NLS, configuration, and LaTeX activation are one suite.                                                                                                                                  | Test architecture issue; static/package contract group is broader than filename                             |
| `extension.test.ts`                                         | Activation, command registration, manifest agreement, and one real conversion journey are one suite.                                                                                                                          | Host integration responsibility mixed                                                                       |
| `split_pdf_all_pages.test.ts` / `merge_pdf_command.test.ts` | PDF content visual comparison is performed through a helper that reads `vscode.workspace` configuration for `pdftocairo`.                                                                                                     | Current runtime is Host, but PDF operation contract may be Node + configured renderer                       |
| `safe_mode_dialog.test.ts` / `safe_mode_status_bar.test.ts` | Host runtime is used while the key VS Code UI surfaces are Sinon fakes.                                                                                                                                                       | Harness-only API adapter evidence, not actual workbench visual evidence                                     |
| Browser spec                                                | One 1,068-line file owns fixture generation, HTTP server, PDF.js assets, renderer layout, browser API compatibility, and simulated Host messages.                                                                             | Browser responsibility mixed; removal decision not made                                                     |
| Electron spec                                               | One 376-line case owns critical journey, Webview rendering, theme, visual snapshots, packaging smoke, native Sharp, internal module smoke, and harness diagnostics.                                                           | Critical journey / visual / packaging / native / internal / harness responsibilities mixed                  |
| `run_external_tool.test.ts`                                 | Name claims `signal`; assertion covers args/stdout/redaction but passes no signal and never observes cancellation.                                                                                                            | Observation mismatch; separate cancellation Evidence is needed                                              |

## Node/Vitest migration candidate selection

These are experiment candidates, not migration decisions. No production/test/config was moved in this audit.

| Priority | Candidate                                                                                                                                                                                                                            | Why value is high                                                                    | Preconditions and experiment oracle                                                                                                                        |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P0       | `source_format.test.ts`, `crop_pdf_protocol.test.ts`, `resolve_output_path.test.ts`                                                                                                                                                  | pure format/protocol/path contracts; fast diagnosis; no external service             | Verify Mocha globals/assertion replacement, platform option typing, malformed input matrix, cold/warm time                                                 |
| P0       | `file_content_hash.test.ts`, `workspace_path.test.ts`                                                                                                                                                                                | safety-critical Node filesystem contracts                                            | Run real temp files and symlink/junction behavior on Linux/macOS/Windows; preserve stream/hash and path-race limitations                                   |
| P0       | `safe_mode.test.ts`                                                                                                                                                                                                                  | pure Safe Mode state/persistence adapter                                             | Keep globalState fake as an adapter oracle; actual profile/globalState behavior remains Host evidence                                                      |
| P0       | `cleanup_conversion_artifacts.test.ts`, `commit_conversion_outputs.test.ts`, `run_staged_conversion_batch.test.ts`                                                                                                                   | transaction/rollback safety is clearer outside VS Code startup                       | Preserve failure injection, AbortSignal, symlink checks, cleanup retry, and output-channel fake; measure diagnosis and duration                            |
| P1       | `run_external_tool.test.ts`                                                                                                                                                                                                          | real child process and redaction boundary                                            | Add a separate signal/cancel case; compare process teardown and diagnostics on 3 OS                                                                        |
| P1       | `convert_to_pdf_operation.test.ts`, `png_safe_mode.test.ts`                                                                                                                                                                          | real file/native conversion plus transaction contract                                | Verify Sharp ABI, fixture path, native codec and timeout behavior on 3 OS; keep UI out of operation evidence                                               |
| P1       | `convert_to_pdf_drawio_path.test.ts`, `convert_to_png_operation.test.ts`, `convert_to_svg_operation.test.ts`                                                                                                                         | operation boundaries use injected tool runners and real file/PDF oracles             | Confirm fake runner preserves failure semantics; distinguish actual CLI probe from operation contract                                                      |
| P1       | `convert_pdftocairo_ascii_scratch.test.ts`, `convert_rsvg_ascii_scratch.test.ts`, `crop_pdf_ghostscript_ascii_scratch.test.ts`                                                                                                       | Windows safety boundary is simulated explicitly and can be diagnosed without VS Code | Run platform simulation plus real tool probe separately; verify scratch base fallback, symlink, cleanup retention                                          |
| P2       | `merge_pdf_operation.test.ts`, `split_pdf_all_pages.test.ts`, `undo_last_conversion.test.ts`, `package_manifest.test.ts`, `check_nls.test.ts`, `webview_html.test.ts`, `output_conversion_messages.test.ts`, `latex_snippet.test.ts` | production boundary or module-scope environment is a prerequisite                    | Separate command records, locale, SnippetString, manifest constants, extension path, renderer settings, and URI/CSP ownership before any runner experiment |

`split_pdf_all_pages.test.ts` and `crop_pdf_configure_operation.test.ts` are also P2 boundary prerequisites because the current visual helper reads `vscode.workspace` configuration. P2 is not a current Node migration recommendation; it records production ownership work required before an experiment.

### Host retention candidates

The following case groups have an actual Host oracle and should remain Host candidates unless a separate contract is introduced: extension discovery/activation; `vscode.commands` registration and execution; workspace configuration updates; `SaveDialog`; `QuickPick`/`InputBox`; notification/progress/cancellation token behavior; `DocumentDropEditProvider` and `DocumentPasteEditProvider`; `DataTransfer`; `globalState`; real VS Code URI/Webview creation; and actual workbench status-bar/command integration. A pure operation assertion in the same file does not inherit this requirement merely because it shares the file.

## Confirmed corrections to earlier foundation hypotheses

- The complete local inventory is 47 implementation files / 226 cases, not an incomplete known-file list.
- Three dedicated ASCII-scratch test files are present: `convert_pdftocairo_ascii_scratch.test.ts`, `convert_rsvg_ascii_scratch.test.ts`, and `crop_pdf_ghostscript_ascii_scratch.test.ts`; shared runner evidence is separate. The old statement that scratch-specific test files had not been acquired is stale.
- `split_pdf_all_pages.test.ts` is currently Host because `pdf_visual_assertions.ts` reads `vscode.workspace` configuration; its operation contract is not thereby proven to require Host.
- `merge_pdf_operation.test.ts`, `undo_last_conversion.test.ts`, `latex_snippet.test.ts`, and `output_conversion_messages.test.ts` have transitive/current `vscode` dependencies even where the assertions look Node-like.
- `run_external_tool.test.ts` does not prove `AbortSignal` cancellation despite its title.
- The Browser spec has 18 cases, not an undifferentiated E2E suite: canvas/PDF.js cases, browser layout/API cases, simulated message cases, and one asset-file existence case have different evidence value.
- The Electron case is real user-journey evidence, but its same test also imports packaged internal modules and verifies Sharp/native/controlled external-fetch behavior. It is not one homogeneous contract.

## Selection status

| Question                                               | Status                                                                                                             |
| ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------ |
| Keep Browser, remove Browser, or replace with Electron | **Experiment required**                                                                                            |
| Adopt Vitest formally                                  | **Experiment required**; current Vitest configured scope is zero and formal adoption remains a maintainer decision |
| Move any test now                                      | **Experiment required**; implementation remains out of scope for this audit                                        |
| Split directories or co-locate tests                   | **Experiment required** after runtime ownership is selected                                                        |
| Treat `test:all` as all required Evidence              | **Ready for decision**; current script excludes Electron, package, and Vitest                                      |
